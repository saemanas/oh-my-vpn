---
title: "Oh My VPN -- Milestone Plan"
release: "MVP (v1.0)"
created_at: "2026-03-04T17:26:00+07:00"
updated_at: "2026-03-05T11:43:00+07:00"
total_milestones: 6
status: "active"
---

# Milestone Plan -- Oh My VPN MVP (v1.0)

## 1. Summary Table

| # | Milestone | Status | Dependencies | Requirements |
| --- | --- | --- | --- | --- |
| M1 | Project Scaffolding + Foundation Modules | completed | -- | FR-PM-3, NFR-SEC-1, NFR-SEC-7 |
| M2 | Provider Manager + Cloud Provider Integration | completed | M1 | FR-PM-1, FR-PM-2, FR-RC-1, FR-RC-2, FR-RC-4, NFR-INT-1 |
| M3 | VPN Manager (WireGuard Integration) | in-progress | M1 | FR-VC-1, FR-VC-2, FR-VC-3, FR-VC-4, FR-VC-5, NFR-SEC-2, NFR-SEC-3, NFR-SEC-6 |
| M4 | Server Lifecycle + Session Tracker | pending | M2, M3 | FR-SL-1, FR-SL-2, FR-SL-3, FR-SL-4, FR-SL-6, FR-SL-7, FR-SS-1, FR-SS-2, NFR-SEC-5, NFR-PERF-1, NFR-PERF-2, NFR-REL-1, NFR-REL-2, NFR-REL-4 |
| M5 | Menu Bar UI -- Core Flow | pending | M4 | FR-MN-1, NFR-SEC-7 |
| M6 | Menu Bar UI -- Onboarding + Context Menu | pending | M5 | FR-OB-1 |

**Execution order with parallelism:**

```plain
M1 → M2 ─┐
     M3 ─┤→ M4 → M5 → M6
   (parallel)
```

---

## 2. Milestone Details

## M1: Project Scaffolding + Foundation Modules

**Status:** `completed`
**Started:** 2026-03-04
**Completed:** 2026-03-04
**Dependencies:** --

### A. Description

Initialize the Tauri project (Rust backend + TypeScript frontend), implement the two foundational backend modules (Keychain Adapter, Preferences Store), set up the Tauri IPC whitelist structure, and configure design tokens CSS. This milestone produces the skeleton that all subsequent milestones build upon.

### B. Modules

| Module | Responsibility |
| --- | --- |
| Keychain Adapter | Implement macOS Security Framework integration -- store, retrieve, delete, list credentials |
| Preferences Store | Implement JSON file-based user preferences -- load, save, schema versioning, atomic writes |
| Tauri IPC | Scaffold command whitelist structure and AppError type |

### C. Requirements Covered

| ID | Description | Priority |
| --- | --- | --- |
| FR-PM-3 | Store API keys in macOS Keychain with encryption | Must |
| NFR-SEC-1 | API keys stored exclusively in macOS Keychain -- zero plaintext keys on disk | Must |
| NFR-SEC-7 | Tauri IPC whitelist -- only required commands exposed (scaffold) | Must |

### D. Acceptance Criteria

- [ ] Tauri project builds and launches on macOS 13+
- [ ] Keychain Adapter stores, retrieves, and deletes credentials via Security Framework
- [ ] Preferences Store reads/writes JSON with schema versioning and atomic file operations
- [ ] Tauri IPC scaffold with AppError type compiles
- [x] Design tokens CSS (tokens.css) integrated into frontend
- [ ] Zero plaintext credentials on disk -- verified by test

### E. Module Breakdown

#### M1.1: Tauri Project Initialization

- [x] **Status**: done (2026-03-04)
- **Scope**: Project root -- `Cargo.toml`, `package.json`, `tauri.conf.json`, `src-tauri/src/main.rs`, `src/`, directory structure
- **Dependencies**: --
- **Required context**:
  - `docs/architecture/containers.md` -- §1-2 (container diagram, descriptions)
  - `docs/architecture/deployment.md` -- §2.A (macOS machine node)
  - `docs/adr/0007-tauri-updater-with-github-releases.md` -- full (Tauri config)
- **Acceptance criteria**:
  - Tauri project created with Rust backend + TypeScript frontend
  - `cargo tauri dev` launches the app on macOS 13+
  - Directory structure matches containers.md module layout
  - Basic `tauri.conf.json` configured (app name, identifier, window settings for menu bar)

#### M1.2: Keychain Adapter

- [x] **Status**: done (2026-03-04)
- **Scope**: `src-tauri/src/keychain_adapter.rs` (or `keychain_adapter/mod.rs`)
- **Dependencies**: M1.1
- **Required context**:
  - `docs/architecture/containers.md` -- §3.F (Keychain Adapter module)
  - `docs/data-model/2026-03-04-1712-data-model.md` -- §4.A (ProviderCredential schema)
  - `docs/api-design/2026-03-04-1726-api-design.md` -- §4.G (MOD-KA Keychain Adapter interface)
  - `docs/architecture/cross-cutting-concepts.md` -- §1 (Credential Security)
- **Acceptance criteria**:
  - `store_credential(provider, account_label, api_key)` writes to macOS Keychain
  - `retrieve_credential(provider)` reads from macOS Keychain
  - `delete_credential(provider)` removes Keychain entry
  - `list_credentials()` returns all registered (provider, account_label) pairs
  - Service name follows pattern `oh-my-vpn.{provider}`
  - Unit tests verify store/retrieve/delete round-trip
  - Zero plaintext credentials on disk -- verified by test

#### M1.3: Preferences Store

- [x] **Status**: completed at 2026-03-04T20:48:00+07:00
- **Scope**: `src-tauri/src/preferences_store.rs` (or `preferences_store/mod.rs`)
- **Dependencies**: M1.1
- **Required context**:
  - `docs/architecture/containers.md` -- §3.G (Preferences Store module)
  - `docs/data-model/2026-03-04-1712-data-model.md` -- §4.B (UserPreferences schema), §6.A (migration strategy)
- **Acceptance criteria**:
  - `load()` reads `preferences.json` from Tauri app data directory
  - `save(preferences)` writes atomically (write tmp + rename)
  - Missing file creates defaults (`schemaVersion: 1`, `notificationsEnabled: true`)
  - Corrupt file backed up to `preferences.backup.json`, replaced with defaults
  - Schema version check present (migration path for future versions)
  - Unit tests verify load/save, defaults creation, corrupt file recovery

#### M1.4: Tauri IPC Scaffold + AppError

- [x] **Status**: completed at 2026-03-04T21:28:00+07:00
- **Scope**: `src-tauri/src/ipc/mod.rs`, `src-tauri/src/error.rs`, `tauri.conf.json` (capabilities), `src-tauri/capabilities/`
- **Dependencies**: M1.1
- **Required context**:
  - `docs/api-design/2026-03-04-1726-api-design.md` -- §6 (AppError format, error code taxonomy)
  - `docs/architecture/containers.md` -- §3.A (Tauri IPC module)
- **Acceptance criteria**:
  - `AppError` struct with `code`, `message`, `details` fields
  - `ProviderError` and `KeychainError` enums with `From<T> for AppError` conversions
  - IPC command registration scaffold (empty handlers that return `AppError`)
  - Tauri v2 capabilities directory structure created
  - Compiles with all error types and IPC scaffold

#### M1.5: Design Tokens CSS

- [x] **Status**: completed at 2026-03-04T21:32:00+07:00
- **Scope**: `src/tokens.css`, `src/main.tsx` (CSS import)
- **Dependencies**: M1.1
- **Required context**:
  - `docs/ui-design/tokens.css` -- full (design token definitions)
  - `docs/ux-design/2026-03-03-1619-ux-design.md` -- §3 (visual foundation)
- **Acceptance criteria**:
  - `tokens.css` from UI design doc integrated into frontend
  - CSS custom properties accessible in the webview
  - Dark mode / light mode tokens respond to `prefers-color-scheme`

**Module execution order:**

```plain
M1.1 → M1.2 ─┐
         M1.3 ─┤ (all parallel after M1.1)
         M1.4 ─┤
         M1.5 ─┘
```

### F. Notes

Preferences Store IPC commands (get_preferences, update_preferences) are deferred to M6 where the settings UI is built. The module itself is implemented here because Server Lifecycle (M4) depends on it to save last-used provider/region.

---

## M2: Provider Manager + Cloud Provider Integration

**Status:** `completed`
**Started:** 2026-03-04T22:35:00+07:00
**Completed:** 2026-03-05T00:55:00+07:00
**Dependencies:** M1

### A. Description

Define the CloudProvider trait and implement all three cloud provider SDKs (Hetzner, AWS, GCP). This covers API key validation, region listing with pricing, SSH key management, and server CRUD operations. Implement IPC commands for provider registration and region listing. Follow Risk R-7 mitigation: complete Hetzner first, then add AWS and GCP sequentially.

### B. Modules

| Module | Responsibility |
| --- | --- |
| Provider Manager | CloudProvider trait definition, provider registry, PricingCache with TTL |
| Hetzner Provider | hcloud-rs SDK integration -- validate, list regions, SSH keys, server CRUD |
| AWS Provider | aws-sdk-ec2 integration -- validate, list regions, key pairs, EC2 instances, security groups |
| GCP Provider | google-cloud-compute-v1 SDK integration -- validate, list regions, SSH metadata, instances, firewall rules |

### C. Requirements Covered

| ID | Description | Priority |
| --- | --- | --- |
| FR-PM-1 | Register API keys for Hetzner, AWS, and GCP | Must |
| FR-PM-2 | Validate API keys against the provider's API upon registration | Must |
| FR-RC-1 | Display available regions for each registered provider | Must |
| FR-RC-2 | Display the hourly cost per region retrieved from provider APIs | Must |
| FR-RC-4 | Select provider and region before server creation | Must |
| NFR-INT-1 | All 3 providers functional | Must |

### D. Acceptance Criteria

- [ ] CloudProvider trait defined with all 7 methods (validate_credential, list_regions, create_ssh_key, delete_ssh_key, create_server, destroy_server, get_server)
- [ ] Hetzner provider passes integration test (validate key, list regions with pricing, server create/destroy)
- [ ] AWS provider passes integration test (same as above)
- [ ] GCP provider passes integration test (same as above)
- [ ] PricingCache implements ~1h TTL with stale fallback on API failure
- [ ] IPC commands register_provider, remove_provider, list_providers, list_regions functional
- [ ] Regions sorted by hourly cost ascending (FR-RC-3 is Should, but sorting is trivial)
- [ ] Invalid API key returns specific error per provider (AUTH_INVALID_KEY, AUTH_INSUFFICIENT_PERMISSIONS)

### E. Module Breakdown

#### M2.1: CloudProvider Trait + Provider Manager Core

- [x] **Status**: completed at 2026-03-04T21:51:00+07:00
- **Scope**: `src-tauri/src/provider_manager/mod.rs`, `src-tauri/src/provider_manager/cloud_provider.rs`, `src-tauri/src/provider_manager/registry.rs`, `src-tauri/src/provider_manager/cache.rs`
- **Dependencies**: M1.2 (Keychain Adapter), M1.4 (AppError)
- **Required context**:
  - `docs/api-design/2026-03-04-1726-api-design.md` -- §4.F (CloudProvider trait definition)
  - `docs/data-model/2026-03-04-1712-data-model.md` -- §4.C (PricingCache schema)
  - `docs/architecture/containers.md` -- §3.B (Provider Manager module)
- **Acceptance criteria**:
  - `CloudProvider` trait with 7 async methods defined
  - `ProviderRegistry` manages provider instances keyed by `Provider` enum
  - `PricingCache` with `HashMap<Provider, CacheEntry>`, TTL check, stale fallback
  - Provider Manager retrieves API keys from Keychain Adapter (not stored in memory)

#### M2.2: Hetzner Provider

- [x] **Status**: completed at 2026-03-04T22:35:00+07:00
- **Scope**: `src-tauri/src/provider_manager/hetzner.rs`
- **Dependencies**: M2.1
- **Required context**:
  - `docs/api-design/2026-03-04-1726-api-design.md` -- §4.F (CloudProvider trait)
  - `docs/adr/0002-use-rust-sdk-for-cloud-providers.md` -- full (Rust SDK decision)
  - `docs/adr/0005-use-provider-pricing-api.md` -- full (Pricing API decision)
  - `docs/architecture/cross-cutting-concepts.md` -- §6.B (cloud-init provider variation)
- **Acceptance criteria**:
  - Implements `CloudProvider` trait using `hcloud` crate
  - `validate_credential`: test API call (e.g., list servers)
  - `list_regions`: returns regions with hourly cost from Hetzner pricing API
  - `create_ssh_key` / `delete_ssh_key`: register/delete SSH key via API
  - `create_server`: provisions cheapest shared server type per location with cloud-init, Ubuntu image
  - `destroy_server` / `get_server`: delete and verify instance
  - Integration test with real Hetzner API (gated by env var)

#### M2.3: AWS Provider

- [x] **Status**: completed at 2026-03-04T22:45:00+07:00
- **Scope**: `src-tauri/src/provider_manager/aws.rs`
- **Dependencies**: M2.1
- **Required context**:
  - `docs/api-design/2026-03-04-1726-api-design.md` -- §4.F (CloudProvider trait)
  - `docs/adr/0002-use-rust-sdk-for-cloud-providers.md` -- full (Rust SDK decision)
  - `docs/adr/0005-use-provider-pricing-api.md` -- full (Pricing API decision)
  - `docs/architecture/cross-cutting-concepts.md` -- §6.B (cloud-init provider variation)
- **Acceptance criteria**:
  - Implements `CloudProvider` trait using `aws-sdk-ec2`
  - `validate_credential`: STS get-caller-identity or EC2 describe-instances
  - `list_regions`: returns regions with hourly cost from EC2 pricing API
  - `create_ssh_key` / `delete_ssh_key`: EC2 key pair management
  - `create_server`: provisions t3.nano with user-data script, security group (WireGuard UDP only)
  - `destroy_server` / `get_server`: terminate and describe instance
  - Integration test with real AWS API (gated by env var)

#### M2.4: GCP Provider

- [x] **Status**: completed at 2026-03-04T23:58:00+07:00
- **Scope**: `src-tauri/src/provider_manager/gcp.rs`
- **Dependencies**: M2.1
- **Required context**:
  - `docs/api-design/2026-03-04-1726-api-design.md` -- §4.F (CloudProvider trait)
  - `docs/adr/0002-use-rust-sdk-for-cloud-providers.md` -- full (Rust SDK decision)
  - `docs/adr/0005-use-provider-pricing-api.md` -- full (Pricing API decision)
  - `docs/architecture/cross-cutting-concepts.md` -- §6.B (cloud-init provider variation)
- **Acceptance criteria**:
  - Implements `CloudProvider` trait using `google-cloud-compute-v1`
  - `validate_credential`: list instances or projects.get
  - `list_regions`: returns zones with hourly cost from Compute Engine pricing
  - `create_ssh_key` / `delete_ssh_key`: SSH metadata management
  - `create_server`: provisions e2-micro with startup script, firewall rule (WireGuard UDP only)
  - `destroy_server` / `get_server`: delete and get instance
  - Integration test with real GCP API (gated by env var)

#### M2.5: Provider Manager IPC Commands

- [x] **Status**: completed at 2026-03-05T00:55:00+07:00
- **Scope**: `src-tauri/src/ipc/provider.rs`, `tauri.conf.json` (capabilities update)
- **Dependencies**: M2.1, M2.2 (at minimum Hetzner for testing)
- **Required context**:
  - `docs/api-design/2026-03-04-1726-api-design.md` -- §3.A (IPC-PM endpoints), §4.B (command schemas), §5.D (registration sequence)
- **Acceptance criteria**:
  - `register_provider` IPC command: validate → store in Keychain → return ProviderInfo
  - `remove_provider` IPC command: delete Keychain entry, invalidate cache
  - `list_providers` IPC command: list from Keychain Adapter
  - `list_regions` IPC command: fetch from Provider Manager with cache
  - Tauri capabilities updated to whitelist these commands
  - Error codes match API design §6.C catalog

**Module execution order:**

```plain
M2.1 → M2.2 → M2.3 → M2.4 (sequential per Risk R-7)
                  └──── M2.5 (after M2.2 at minimum, tests with all providers after M2.4)
```

### F. Notes

Provider-specific cloud-init scripts are defined here (cross-cutting §6) but the provisioning orchestration is in M4. SSH key management (create_ssh_key, delete_ssh_key) is exposed via the trait here but orchestrated by Server Lifecycle in M4. Risk R-7 mitigation: complete Hetzner first, validate the trait interface works, then add AWS and GCP.

---

## M3: VPN Manager (WireGuard Integration)

**Status:** `in-progress`
**Started:** 2026-03-05T01:29:00+07:00
**Completed:** --
**Dependencies:** M1

### A. Description

Implement WireGuard tunnel management -- ephemeral key pair generation, config file lifecycle (write with permission 600, delete after use), wg-quick subprocess execution via sudo, and DNS leak prevention. This milestone is independent from Provider Manager (M2) and can be developed in parallel.

### B. Modules

| Module | Responsibility |
| --- | --- |
| VPN Manager | WireGuard key generation (Curve25519), config file management, wg-quick up/down subprocess, DNS routing configuration |

### C. Requirements Covered

| ID | Description | Priority |
| --- | --- | --- |
| FR-VC-1 | Generate WireGuard key pairs locally per session | Must |
| FR-VC-2 | Establish WireGuard tunnel to the provisioned server | Must |
| FR-VC-3 | Tear down WireGuard tunnel on user disconnect | Must |
| FR-VC-4 | Delete local WireGuard keys after tunnel teardown and server destruction | Must |
| FR-VC-5 | Route DNS queries through the VPN tunnel to prevent DNS leaks | Must |
| NFR-SEC-2 | Ephemeral WireGuard keys -- generated per session, deleted on teardown | Must |
| NFR-SEC-3 | DNS queries route through VPN tunnel during active session -- zero DNS leak | Must |
| NFR-SEC-6 | WireGuard config files have permission 600 and are deleted after use | Must |

### D. Acceptance Criteria

- [ ] WireGuard key pair generated using x25519_dalek or equivalent
- [ ] Config file written to /tmp with permission 600 -- verified by test
- [ ] Config file deleted immediately after wg-quick up -- regardless of success or failure
- [ ] wg-quick up/down executed via sudo osascript
- [ ] DNS server set to VPN server's resolver in WireGuard config
- [ ] Key pair zeroed from memory after tunnel teardown (zeroize crate)
- [ ] Bundle wireguard-go and wg-quick in Tauri app (ADR-0001)
- [ ] Integration test: tunnel up → DNS resolves through VPN → tunnel down → keys zeroed

### E. Module Breakdown

#### M3.1: WireGuard Key Generation

- [x] **Status**: completed at 2026-03-05T01:29+07:00
- **Scope**: `src-tauri/src/vpn_manager/keys.rs`
- **Dependencies**: M1.1
- **Required context**:
  - `docs/data-model/2026-03-04-1712-data-model.md` -- §4.C (WireGuardKeyPair schema)
- **Acceptance criteria**:
  - Generate Curve25519 key pair using `x25519_dalek` crate
  - Keys implement `zeroize::Zeroize` for secure memory cleanup
  - Public key encoded to base64 for WireGuard config
  - Unit test: generate → verify valid key pair → zeroize → verify zeroed

#### M3.2: WireGuard Config File Management

- [x] **Status**: completed at 2026-03-05T11:43:00+07:00
- **Scope**: `src-tauri/src/vpn_manager/config.rs`
- **Dependencies**: M3.1
- **Required context**:
  - `docs/data-model/2026-03-04-1712-data-model.md` -- §4.C (WireGuardConfig schema)
  - `docs/adr/0001-use-wireguard-go-with-wg-quick.md` -- full (wg-quick decision)
- **Acceptance criteria**:
  - Write standard WireGuard INI config to `/tmp/oh-my-vpn-wg0.conf`
  - File permission set to `0o600` immediately after write
  - DNS field populated with VPN server resolver (FR-VC-5)
  - AllowedIPs set to `0.0.0.0/0, ::/0` (route all traffic)
  - Delete function removes file (called regardless of wg-quick success/failure)
  - Unit test: write → verify permission 600 → delete → verify file gone

#### M3.3: wg-quick Subprocess + Sidecar Bundling

- [ ] **Status**: pending
- **Scope**: `src-tauri/src/vpn_manager/tunnel.rs`, `src-tauri/binaries/` (wireguard-go, wg-quick sidecar)
- **Dependencies**: M3.2, M1.1 (Tauri sidecar config)
- **Required context**:
  - `docs/adr/0001-use-wireguard-go-with-wg-quick.md` -- full (wireguard-go + wg-quick)
  - `docs/adr/0003-no-network-extension-for-mvp.md` -- full (no Network Extension)
  - `docs/architecture/cross-cutting-concepts.md` -- §10 (System Permissions, sudo via osascript)
- **Acceptance criteria**:
  - `tunnel_up(server_ip, server_public_key)`: write config → `sudo wg-quick up` via osascript → delete config
  - `tunnel_down()`: `sudo wg-quick down` via osascript → zero keys
  - wireguard-go and wg-quick binaries bundled as Tauri sidecar
  - Config file deleted in both success and failure paths (try/finally pattern)
  - Integration test: tunnel up → verify interface exists → tunnel down → verify interface removed

**Module execution order:**

```plain
M3.1 → M3.2 → M3.3 (sequential -- each builds on the previous)
```

### F. Notes

VPN Manager exposes a `tunnel_up(server_ip, server_public_key)` and `tunnel_down()` interface. It does not know about cloud providers -- Server Lifecycle (M4) orchestrates the connection between Provider Manager and VPN Manager. The wireguard-go and wg-quick binaries must be bundled in the Tauri sidecar per ADR-0001.

---

## M4: Server Lifecycle + Session Tracker

**Status:** `pending`
**Started:** --
**Completed:** --
**Dependencies:** M2, M3

### A. Description

Implement the central orchestrator (Server Lifecycle) that coordinates Provider Manager and VPN Manager for the full connect/disconnect flow. Implement Session Tracker for session state persistence and orphaned server detection. This is the largest milestone -- it wires together all backend modules into the complete server provisioning, connection, destruction, and recovery flows.

### B. Modules

| Module | Responsibility |
| --- | --- |
| Server Lifecycle | Orchestrate connect flow (SSH key → server provision → WireGuard tunnel), disconnect flow (tunnel down → destroy → verify deletion), auto-cleanup on failure |
| Session Tracker | Persist active session state (active-session.json), detect orphaned servers on app launch, report session status (IP, elapsed time, cost) |

### C. Requirements Covered

| ID | Description | Priority |
| --- | --- | --- |
| FR-SL-1 | Provision a cloud server with WireGuard via cloud-init on user request | Must |
| FR-SL-2 | Configure firewall rules during provisioning -- WireGuard UDP only | Must |
| FR-SL-3 | Destroy the server via provider API on user disconnect | Must |
| FR-SL-4 | Auto-cleanup a partially created server on provisioning failure | Must |
| FR-SL-6 | Detect orphaned servers from previous sessions on app launch | Must |
| FR-SL-7 | Prompt the user to destroy or reconnect to a detected orphaned server | Must |
| FR-SS-1 | Display the connected server's public IP during an active session | Must |
| FR-SS-2 | Display elapsed connection time during an active session | Must |
| NFR-SEC-5 | Provisioned server firewall allows only WireGuard UDP port | Must |
| NFR-PERF-1 | Server provisioning to WireGuard connection established ≤ 120 seconds | Must |
| NFR-PERF-2 | Disconnect to server destruction confirmed ≤ 30 seconds | Must |
| NFR-REL-1 | Detect orphaned servers on launch -- 100% detection across all providers | Must |
| NFR-REL-2 | Failed provisioning triggers auto-cleanup -- zero orphaned servers | Must |
| NFR-REL-4 | App crash does not leave WireGuard tunnel active without user awareness | Must |

### D. Acceptance Criteria

- [ ] Connect flow completes: SSH key gen → register → server provision → SSH key delete → WireGuard tunnel up → session file created → preferences updated
- [ ] Disconnect flow completes: tunnel down → server destroy → verify deletion → session file deleted → keys zeroed
- [ ] Auto-cleanup on any failure mid-connect: server destroyed, SSH key deleted, WireGuard keys zeroed
- [ ] Orphan detection on app launch: read session file → query provider API → return orphan list
- [ ] Resolve orphan: destroy (with verification) or reconnect (new WireGuard keys)
- [ ] Session status returns live-calculated elapsed time and accumulated cost
- [ ] Destruction verification: re-check server existence via get_server after destroy_server
- [ ] Retry destruction up to 3 times with exponential backoff on failure
- [ ] IPC commands functional: connect, disconnect, check_orphaned_servers, resolve_orphaned_server, get_session_status
- [ ] End-to-end test with real Hetzner API: connect → verify IP → disconnect → verify destruction

### E. Module Breakdown

#### M4.1: Session Tracker

- [ ] **Status**: pending
- **Scope**: `src-tauri/src/session_tracker.rs` (or `session_tracker/mod.rs`)
- **Dependencies**: M1.1
- **Required context**:
  - `docs/architecture/containers.md` -- §3.E (Session Tracker module)
  - `docs/data-model/2026-03-04-1712-data-model.md` -- §4.B (ActiveSession schema), §5.B (access patterns)
- **Acceptance criteria**:
  - `create_session(server_info, hourly_cost)`: write `active-session.json` atomically
  - `read_session()`: read and parse session file, return `None` if missing
  - `delete_session()`: remove session file
  - `get_status()`: return SessionStatus with live-calculated elapsed time and accumulated cost
  - Atomic writes (write tmp + rename) to prevent partial state on crash
  - Unit tests verify create/read/delete round-trip and atomic write behavior

#### M4.2: Server Lifecycle -- Connect Flow

- [ ] **Status**: pending
- **Scope**: `src-tauri/src/server_lifecycle/mod.rs`, `src-tauri/src/server_lifecycle/connect.rs`
- **Dependencies**: M2.1 (Provider Manager), M3.3 (VPN Manager tunnel), M4.1 (Session Tracker), M1.3 (Preferences Store)
- **Required context**:
  - `docs/api-design/2026-03-04-1726-api-design.md` -- §4.C (connect command schema), §5.A (connect sequence diagram)
  - `docs/architecture/cross-cutting-concepts.md` -- §1 (Credential Security, ephemeral keys)
  - `docs/adr/0004-ephemeral-ssh-keys-per-session.md` -- full (SSH key strategy)
  - `docs/data-model/2026-03-04-1712-data-model.md` -- §4.C (SshKeyPair schema)
- **Acceptance criteria**:
  - Orchestrates 11-step connect flow from API design §4.C
  - Generates ephemeral SSH key pair (Ed25519) via `ssh-key` or `ed25519_dalek`
  - SSH key registered with provider, deleted after cloud-init completes
  - Server provisioned with cloud-init script (provider-specific)
  - WireGuard tunnel established via VPN Manager
  - Session file created via Session Tracker
  - Preferences updated (lastProvider, lastRegion)
  - SSH keys zeroed from memory after cleanup

#### M4.3: Server Lifecycle -- Disconnect Flow

- [ ] **Status**: pending
- **Scope**: `src-tauri/src/server_lifecycle/disconnect.rs`
- **Dependencies**: M4.2 (shares Server Lifecycle module), M2.1, M3.3, M4.1
- **Required context**:
  - `docs/api-design/2026-03-04-1726-api-design.md` -- §4.C (disconnect command schema), §5.B (disconnect sequence diagram)
  - `docs/architecture/cross-cutting-concepts.md` -- §8 (disconnect flow with verification), §2 (error handling and retry)
- **Acceptance criteria**:
  - Orchestrates disconnect: tunnel down → destroy server → verify deletion → clear session
  - Deletion verification: re-check via `get_server()` after `destroy_server()`
  - Retry destruction up to 3 times with exponential backoff
  - On persistent failure: return error with manual deletion guide (provider console URL)
  - WireGuard keys zeroed from memory

#### M4.4: Server Lifecycle -- Auto-Cleanup + Orphan Detection

- [ ] **Status**: pending
- **Scope**: `src-tauri/src/server_lifecycle/cleanup.rs`, `src-tauri/src/server_lifecycle/orphan.rs`
- **Dependencies**: M4.2, M4.3, M4.1
- **Required context**:
  - `docs/architecture/cross-cutting-concepts.md` -- §3 (orphaned server recovery)
  - `docs/api-design/2026-03-04-1726-api-design.md` -- §4.C (orphan commands), §5.C (orphan detection sequence)
- **Acceptance criteria**:
  - Auto-cleanup on connect failure: destroy server if created, delete SSH key if registered, zero keys
  - `check_orphaned_servers()`: read session file → query provider → return orphan list
  - `resolve_orphaned_server(action)`: destroy or reconnect
  - Orphan detection runs asynchronously (does not block app launch)
  - 100% detection rate for persisted sessions

#### M4.5: Server Lifecycle IPC Commands

- [ ] **Status**: pending
- **Scope**: `src-tauri/src/ipc/server.rs`, `src-tauri/src/ipc/session.rs`, capabilities update
- **Dependencies**: M4.2, M4.3, M4.4, M4.1
- **Required context**:
  - `docs/api-design/2026-03-04-1726-api-design.md` -- §3.B (IPC-SL endpoints), §3.C (IPC-SS endpoints), §4.C (SL command schemas), §4.D (SS command schemas)
- **Acceptance criteria**:
  - `connect` IPC command: delegates to Server Lifecycle connect flow
  - `disconnect` IPC command: delegates to disconnect flow
  - `check_orphaned_servers` IPC command: delegates to orphan detection
  - `resolve_orphaned_server` IPC command: delegates to orphan resolution
  - `get_session_status` IPC command: delegates to Session Tracker
  - Tauri capabilities updated to whitelist these commands
  - Error codes match API design §6.C catalog

**Module execution order:**

```plain
M4.1 → M4.2 → M4.3 → M4.4 (sequential -- each builds on previous)
                         └── M4.5 (after all logic modules)
```

### F. Notes

This milestone's connect flow follows the 11-step sequence defined in API Design §4.C (connect command). The disconnect flow follows the verification pattern in cross-cutting concepts §8. Cloud-init scripts per provider are authored in M2 but orchestrated here. The SshKeyPair entity (data model §4.C) is managed entirely within Server Lifecycle -- generated before provisioning, provider-registered, then deleted after cloud-init completes.

---

## M5: Menu Bar UI -- Core Flow

**Status:** `pending`
**Started:** --
**Completed:** --
**Dependencies:** M4

### A. Description

Build the main Menu Bar UI with the core VPN flow: popover shell with stack navigation, disconnected view (provider selector, region list with cost, connect button), connected view (session card with IP/time/cost, disconnect button), provisioning stepper (3-step progress), destruction confirm dialog, error display patterns, and menu bar icon with 3 states. Integrate Liquid Glass design system, dark/light mode, and wire all views to backend IPC commands.

### B. Modules

| Module | Responsibility |
| --- | --- |
| Menu Bar UI | Popover structure, stack navigation, all core flow views, menu bar icon with status states, Liquid Glass styling |

### C. Requirements Covered

| ID | Description | Priority |
| --- | --- | --- |
| FR-MN-1 | Menu bar icon reflecting VPN status (disconnected, connecting, connected) | Must |
| NFR-SEC-7 | Tauri IPC whitelist -- all commands verified against allowlist (complete enforcement) | Must |

### D. Acceptance Criteria

- [ ] Popover opens on menu bar left-click with 320px fixed width
- [ ] Disconnected view: provider selector (if multiple), region list sorted by cost, connect button
- [ ] Connected view: session card (IP in SF Mono 22px, region + provider, elapsed time, cost), disconnect button
- [ ] Provisioning stepper: 3 steps (creating server → installing WireGuard → connecting tunnel) with done/active/waiting/failed states
- [ ] Destruction confirm dialog: modal over blur overlay, Cancel + Destroy buttons
- [ ] Error display: error glass card with retry option on provisioning failure
- [ ] Menu bar icon shows 3 states: disconnected, connecting (animated), connected
- [ ] Stack navigation with spring easing horizontal slide (650ms)
- [ ] Dark mode and light mode automatic via prefers-color-scheme
- [ ] Liquid Glass 4-layer sandwich applied to buttons and cards
- [ ] Keyboard navigation: Tab/Shift+Tab, Enter/Space, Esc to close
- [ ] All IPC commands match Tauri capabilities whitelist (fitness function S-3 pattern)
- [ ] Reduced motion: all animations → fadeIn 200ms when prefers-reduced-motion active

### E. Module Breakdown

#### M5.1: Popover Shell + Stack Navigation

- [ ] **Status**: pending
- **Scope**: `src/components/`, `src/App.tsx`, Tauri window config for menu bar popover
- **Dependencies**: M1.5 (Design Tokens)
- **Required context**:
  - `docs/ux-design/2026-03-03-1619-ux-design.md` -- §4 (core flow user journey)
  - `docs/ui-design/2026-03-04-0123-ui-design.md` -- §3 (layout system), §4.A (stack navigation spec)
- **Acceptance criteria**:
  - Popover opens on menu bar icon left-click (320px fixed width)
  - Stack-based navigation (push/pop) with spring easing horizontal slide (650ms)
  - Esc key closes popover
  - Reduced motion: animations → fadeIn 200ms
  - Shell component renders child views based on navigation state

#### M5.2: Disconnected View

- [ ] **Status**: pending
- **Scope**: `src/components/DisconnectedView.tsx`, `src/components/RegionList.tsx`, `src/components/ProviderSelector.tsx`
- **Dependencies**: M5.1, M2.5 (list_providers, list_regions IPC)
- **Required context**:
  - `docs/ux-design/2026-03-03-1619-ux-design.md` -- §4.B (disconnected state)
  - `docs/ui-design/2026-03-04-0123-ui-design.md` -- §4.B (region list wireframe)
  - `docs/api-design/2026-03-04-1726-api-design.md` -- §4.B (list_providers, list_regions commands)
- **Acceptance criteria**:
  - Provider selector visible only when multiple providers registered
  - Region list sorted by hourly cost ascending
  - Each region row shows: region name, instance type, hourly cost
  - Connect button triggers `connect` IPC command
  - Last-used region pre-selected from preferences (expert shortcut)
  - Liquid Glass styling on region rows and connect button

#### M5.3: Connected View + Session Card

- [ ] **Status**: pending
- **Scope**: `src/components/ConnectedView.tsx`, `src/components/SessionCard.tsx`
- **Dependencies**: M5.1, M4.5 (get_session_status, disconnect IPC)
- **Required context**:
  - `docs/ux-design/2026-03-03-1619-ux-design.md` -- §4.C (connected state)
  - `docs/ui-design/2026-03-04-0123-ui-design.md` -- §4.C (session card wireframe)
  - `docs/api-design/2026-03-04-1726-api-design.md` -- §4.D (get_session_status command)
- **Acceptance criteria**:
  - Session card displays: IP (SF Mono 22px), region + provider, elapsed time, cost
  - Periodic polling of `get_session_status` for live updates
  - Disconnect button triggers confirmation dialog (M5.5)
  - Liquid Glass styling on session card

#### M5.4: Provisioning Stepper

- [ ] **Status**: pending
- **Scope**: `src/components/ProvisioningStepper.tsx`
- **Dependencies**: M5.1
- **Required context**:
  - `docs/ux-design/2026-03-03-1619-ux-design.md` -- §4.D (provisioning flow)
  - `docs/ui-design/2026-03-04-0123-ui-design.md` -- §4.D (stepper wireframe)
- **Acceptance criteria**:
  - 3 steps: creating server → installing WireGuard → connecting tunnel
  - Each step has 4 states: done (checkmark), active (spinner), waiting (dim), failed (error icon)
  - Transitions driven by connect IPC progress
  - On failure: error glass card with retry option

#### M5.5: Destruction Confirm Dialog + Error Display

- [ ] **Status**: pending
- **Scope**: `src/components/ConfirmDialog.tsx`, `src/components/ErrorCard.tsx`
- **Dependencies**: M5.1
- **Required context**:
  - `docs/architecture/cross-cutting-concepts.md` -- §8.A (confirmation dialog), §2 (error handling)
  - `docs/ui-design/2026-03-04-0123-ui-design.md` -- §4.F (dialog wireframe)
- **Acceptance criteria**:
  - Modal dialog over blur overlay
  - Cancel (neutral) + Destroy (destructive red) buttons
  - Error glass card: error message + retry button
  - Keyboard accessible: Enter confirms, Esc cancels

#### M5.6: Menu Bar Icon

- [ ] **Status**: pending
- **Scope**: `src-tauri/src/tray.rs` (or tray module), icon assets
- **Dependencies**: M1.1
- **Required context**:
  - `docs/ux-design/2026-03-03-1619-ux-design.md` -- §4.A (icon states)
  - `docs/ui-design/2026-03-04-0123-ui-design.md` -- §4.A (icon spec)
- **Acceptance criteria**:
  - 3 icon states: disconnected, connecting (animated), connected
  - Left-click opens popover
  - Right-click opens context menu (placeholder for M6)
  - Icon updates based on VPN state

**Module execution order:**

```plain
M5.1 → M5.2 ─┐
         M5.3 ─┤ (parallel after M5.1)
         M5.4 ─┤
         M5.5 ─┤
         M5.6 ─┘
```

### F. Notes

The frontend polls get_session_status periodically to update the session card. The connect IPC command returns SessionStatus on success which populates the initial session card. Provider selector is visible only when multiple providers are registered (from list_providers). Last-used region pre-selected from preferences (expert shortcut -- UX Design §4.E). Orphaned server notification is displayed here after check_orphaned_servers returns results.

---

## M6: Menu Bar UI -- Onboarding + Context Menu

**Status:** `pending`
**Started:** --
**Completed:** --
**Dependencies:** M5

### A. Description

Build the first-run onboarding flow (welcome → provider selection → API key input with validation), the right-click context menu (provider management, system permissions, settings, about, quit), and the quit-while-connected destruction confirmation. Wire Preferences Store IPC commands (get_preferences, update_preferences) for settings persistence.

### B. Modules

| Module | Responsibility |
| --- | --- |
| Menu Bar UI | Onboarding views (3 screens with stack navigation), right-click context menu, settings view, quit-while-connected handler |

### C. Requirements Covered

| ID | Description | Priority |
| --- | --- | --- |
| FR-OB-1 | First-run onboarding flow guiding provider selection and API key input | Must |

### D. Acceptance Criteria

- [ ] Onboarding detects first run (no providers registered via list_providers)
- [ ] Welcome screen → provider selection → API key input with stack navigation
- [ ] API key validation: loading state on button, input disabled during validation, inline error/success
- [ ] Provider-specific API key issuance summary with external doc link
- [ ] Skip to complete onboarding with one provider registered
- [ ] Right-click context menu: provider management, system permissions, settings, about, quit
- [ ] Quit while connected: intercept quit → show destruction confirm dialog → full disconnect flow → exit
- [ ] IPC commands get_preferences, update_preferences functional
- [ ] Settings view: notification toggle, keyboard shortcut configuration

### E. Module Breakdown

#### M6.1: Onboarding Flow

- [ ] **Status**: pending
- **Scope**: `src/components/onboarding/WelcomeScreen.tsx`, `ProviderSelection.tsx`, `ApiKeyInput.tsx`
- **Dependencies**: M5.1 (stack navigation), M2.5 (register_provider, list_providers IPC)
- **Required context**:
  - `docs/ux-design/2026-03-03-1619-ux-design.md` -- §5 (onboarding journey)
  - `docs/ui-design/2026-03-04-0123-ui-design.md` -- §5 (onboarding wireframes)
  - `docs/api-design/2026-03-04-1726-api-design.md` -- §5.D (registration sequence)
- **Acceptance criteria**:
  - First-run detection: `list_providers` returns empty array → show onboarding
  - Welcome screen with product intro
  - Provider selection (Hetzner, AWS, GCP cards)
  - API key input with inline validation (loading, error, success states)
  - Provider-specific help text with external doc link
  - Skip after first provider registered
  - Stack navigation between screens

#### M6.2: Preferences IPC Commands

- [ ] **Status**: pending
- **Scope**: `src-tauri/src/ipc/preferences.rs`, capabilities update
- **Dependencies**: M1.3 (Preferences Store module)
- **Required context**:
  - `docs/api-design/2026-03-04-1726-api-design.md` -- §3.D (IPC-PS endpoints), §4.E (preferences command schemas)
- **Acceptance criteria**:
  - `get_preferences` IPC command: delegates to Preferences Store
  - `update_preferences` IPC command: partial merge, atomic write, return full prefs
  - Tauri capabilities updated
  - `schemaVersion` not exposed to frontend

#### M6.3: Context Menu + Settings View

- [ ] **Status**: pending
- **Scope**: `src-tauri/src/tray.rs` (context menu items), `src/components/SettingsView.tsx`
- **Dependencies**: M5.6 (tray module), M6.2 (preferences IPC)
- **Required context**:
  - `docs/ux-design/2026-03-03-1619-ux-design.md` -- §7 (context menu)
  - `docs/ui-design/2026-03-04-0123-ui-design.md` -- §7 (settings wireframe)
  - `docs/architecture/cross-cutting-concepts.md` -- §10 (system permissions)
- **Acceptance criteria**:
  - Right-click context menu: provider management, system permissions, settings, about, quit
  - Settings view: notification toggle, keyboard shortcut configuration
  - Provider management: list registered providers, add/remove
  - System permissions: show sudo status info
  - About: app version, GitHub link

#### M6.4: Quit-While-Connected Handler

- [ ] **Status**: pending
- **Scope**: `src-tauri/src/lifecycle.rs` (or app lifecycle handler)
- **Dependencies**: M5.5 (confirm dialog), M4.3 (disconnect flow)
- **Required context**:
  - `docs/architecture/cross-cutting-concepts.md` -- §9 (app quit while connected)
- **Acceptance criteria**:
  - Intercept Tauri `close_requested` or `quit` lifecycle event
  - If session active: show destruction confirm dialog
  - On confirm: run full disconnect-and-destroy flow → then exit
  - On cancel: abort quit, resume session

**Module execution order:**

```plain
M6.1 ─┐
M6.2 ─┤ (parallel -- no mutual dependencies)
M6.3 ─┤ (depends on M6.2 for settings persistence)
M6.4 ─┘
```

### F. Notes

Onboarding reuses the same IPC commands as the core flow (register_provider, list_providers). The right-click context menu is a native Tauri system tray context menu -- not a popover view. Quit-while-connected follows cross-cutting concepts §9: intercept Tauri lifecycle event, present confirmation, run full disconnect-and-destroy before process exit.

---

## 3. Requirement Coverage Matrix

### A. Must Functional Requirements (21)

| ID | Milestone |
| --- | --- |
| FR-PM-1 | M2 |
| FR-PM-2 | M2 |
| FR-PM-3 | M1 |
| FR-RC-1 | M2 |
| FR-RC-2 | M2 |
| FR-RC-4 | M2 |
| FR-SL-1 | M4 |
| FR-SL-2 | M4 |
| FR-SL-3 | M4 |
| FR-SL-4 | M4 |
| FR-SL-6 | M4 |
| FR-SL-7 | M4 |
| FR-VC-1 | M3 |
| FR-VC-2 | M3 |
| FR-VC-3 | M3 |
| FR-VC-4 | M3 |
| FR-VC-5 | M3 |
| FR-SS-1 | M4 |
| FR-SS-2 | M4 |
| FR-MN-1 | M5 |
| FR-OB-1 | M6 |

### B. Must Non-Functional Requirements (12)

| ID | Milestone |
| --- | --- |
| NFR-PERF-1 | M4 |
| NFR-PERF-2 | M4 |
| NFR-SEC-1 | M1 |
| NFR-SEC-2 | M3 |
| NFR-SEC-3 | M3 |
| NFR-SEC-5 | M4 |
| NFR-SEC-6 | M3 |
| NFR-SEC-7 | M1 (scaffold) + M5 (enforce) |
| NFR-REL-1 | M4 |
| NFR-REL-2 | M4 |
| NFR-REL-4 | M4 |
| NFR-INT-1 | M2 |

### C. Coverage Note

PRD §7.A states "Must FR: 20, Must NFR: 11, Total: 31" but the actual count from §6.A and §6.B tables is Must FR: 21, Must NFR: 12, Total: 33. All 33 Must requirements are covered by this milestone plan.

---
